import { NgModelDirective } from './ng-model.directive';

describe('NgModelDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelDirective();
    expect(directive).toBeTruthy();
  });
});
